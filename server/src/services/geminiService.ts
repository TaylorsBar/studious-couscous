// server/src/services/geminiService.ts\n\nimport { Gemini } from '@google-cloud/generative-ai'; // Make sure to install the SDK\n\nconst gemini = new Gemini({\n    apiKey: process.env.GEMINI_API_KEY,\n});\n\nexport async function validatePlan(input: string): Promise<{ status: string; result: string; feedback: string }> {\n    try {\n        const response = await gemini.generateResponse(input);\n        return {\n            status: 'success',\n            result: response.result,\n            feedback: 'Plan validated successfully.',\n        };\n    } catch (error) {\n        console.error('Error validating plan:', error);\n        return {\n            status: 'error',\n            result: '',\n            feedback: 'An error occurred while validating the plan.',\n        };\n    }\n}